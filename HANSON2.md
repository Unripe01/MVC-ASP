# HANSON2.md

# MVC + htmx + Alpine.js ハンズオン2

## 全体所要時間（目安）

このハンズオンの総所要時間は約 12 時間です。

- STEP 0: 30分
- STEP 1: 90分
- STEP 2: 120分
- STEP 3: 120分
- STEP 4: 90分
- STEP 5: 150分
- STEP 6: 120分

合計: 720分（約12時間）

## このドキュメントの役割

この資料は「概念説明」ではなく、
実際に手を動かして、壊して、回収して、設計判断まで説明できるようになるための実装手順書です。

HANSON1 で完成した現在のコードベースを出発点として、
同じ画面を段階的に進化させます。

目標は「ライブラリを使えること」ではなく、次を説明できることです。

- なぜ普通のMVCは保守しやすいのか
- なぜ jQuery Ajax は最初に速く、後で壊れやすいのか
- htmx が改善する範囲と、改善しない範囲はどこか
- Alpine.js に任せるべき責務の上限はどこか
- どんな条件で再び Ajax 地獄になるのか

---

## 先に読む: このハンズオンの進め方

### 1. この順番は守る

STEP 1 から順番に進めてください。
途中のSTEPを飛ばすと「なぜ破綻するか」の体験が抜け、理解が浅くなります。

### 2. 毎STEPで必ず4点を実施

- 実装する
- 動作確認する
- 意図的に壊す
- 回収して安定化する

### 3. 1STEPごとに学習ログを書く

最後にまとめて書くのではなく、STEP完了時に必ず書いてください。
理解は「記録した瞬間」に定着します。

### 4. 迷ったら責務で判断する

迷った時は次の基準に戻ること。

- サーバー: 業務ルール、Validation、権限制御、永続化
- htmx: 通信配線、部分更新、サーバーHTML差し替え
- Alpine: 開閉、表示切替、一時的UI状態

---

## 対象読者

- HANSON1 を完了し、MVCの基本を一通り触った人
- Controller / Service / Repository の責務を初歩レベルで理解している人
- Ajax の辛さを体験で理解したい人
- React/Vue の前にサーバーHTML主体の設計を身につけたい人

---

## 前提条件

- 開始元ブランチは hanson であること
- 作業ブランチとして hanson2 を新規作成していること
- HANSON1 の最終状態（ユーザー登録ボタンとユーザーリスト表示）が動作すること
- .NET MVCプロジェクトをローカルで起動できること
- ブラウザ開発者ツールの Network タブを開けること

### 開始時点の実装事実（コード確認済み）

このハンズオン2は、次の状態から開始する。

- HomeController の Index で Repository からユーザー一覧を取得して View へ返している
- HomeController の Create は GET/POST があり、POST 後に Index へリダイレクトしている
- Home/Index には「登録」ボタンがあり、一覧（User List）が表示される

### 開始前チェック（必須）

1. ブランチを hanson に切り替える。
2. hanson2 ブランチを新規作成して切り替える。
3. アプリを起動し、Home 画面で次を確認する。
  - 「登録」ボタンが表示される
  - User List の見出しと一覧が表示される
4. 登録画面から1件追加して Index に戻ることを確認する。

この4つが通らない場合は、ハンズオン2に進まず HANSON1 側の状態を先に揃える。

---

## 最終成果物（ハンズオン2の到達点）

1つの検索画面を完成させます。

最終機能:

- 名前検索
- 一覧表示
- 詳細モーダル表示
- 編集モード切替
- インライン更新
- バリデーションエラー表示

最終責務:

- サーバー: 業務判定、Validation、権限、DB整合性
- htmx: 部分更新通信、PartialView差し替え
- Alpine: UIローカル状態のみ

---

## 全体マップ（最初に全体像を把握する）

### STEP 0: 現状棚卸し（Home基準線固定）

HANSON1成果物を確認し、変更前の基準線を固定する。

### STEP 1: 普通のMVCで基準線を移植する

全画面リロード方式で検索と表示を安定動作させる。

### STEP 2: jQuery Ajax化

部分更新を入れて便利さを得る。同時に責務分散の種を作る。

### STEP 3: htmx導入

jQueryの通信配線をHTML属性へ戻し、更新責務を可視化する。

### STEP 4: Alpine導入

UI状態だけをAlpineで持たせる。

### STEP 5: 破綻実験

3種類の破綻を意図的に起こして、原因と再発条件を言語化する。

### STEP 6: 回収と固定

破綻を解消し、保守可能な構成として完成させる。

---

## 運用補助資料

日々の実装で使いやすいように、次の2つを追加しています。

- 1ページ実装チェックシート: [HANSON2-CHECKSHEET.md](HANSON2-CHECKSHEET.md)
- STEP完了判定の運用版: [HANSON2-DONE-CRITERIA.md](HANSON2-DONE-CRITERIA.md)

---

## STEP 0: 現状棚卸し（Home基準線固定 / 30分）

### 目的

後で比較できるよう、HANSON1最終状態を「基準線」として固定する。

### 作業

1. hanson ブランチで HANSON1成果物を確認後、hanson2 ブランチへ切り替えて起動する。
2. Home/Index を開き、登録ボタンとユーザー一覧表示を確認する。
3. Home/Create でユーザーを1件登録し、Index に戻る流れを確認する。
4. 現在の画面遷移と機能を箇条書きで記録する。
5. 変更対象になりそうなファイルを洗い出す。

想定対象（現コード起点）:

- HomeController
- UserRepository / IUserRepository
- User Model
- Home/Index, Home/Create
- 今後追加する User 系 Controller / ViewModel / View / PartialView
- site.js（jQuery段階で利用）

6. 次の観点で現状メモを作る。

- どこで一覧取得しているか
- どこで一覧を描画しているか
- どこでValidationしているか
- どこで状態を保持しているか

### 完了条件

- Home 起点の現状処理フローを 5〜10行で説明できる
- 変更候補ファイルの一覧がある
- HANSON1最終状態の確認結果（登録ボタン/一覧/登録導線）が記録されている

### 学習ログテンプレ

1. 現状で分かった責務配置
2. 不安がある箇所
3. 次STEPで確認したい点

---

## STEP 1: 普通のMVCで基準線を移植する（90分）

### 目的

Home 基準線を保ったまま、ハンズオン2用の入口（User）へ責務を移植する。

### 実装タスク

1. Home の現状機能（登録導線と一覧表示）を壊さないことを先に確認する。
2. 画面入口を GET /User/Index として新設する。
3. 検索を POST /User/Search で受ける。
4. ViewModelを使って同一Viewを再描画する。
5. 更新系（登録/編集）がある場合は PRG を徹底する。

### 実装時のルール

- Controller は薄く保つ
- 条件分岐は Service に寄せる
- View に業務ルールを書かない
- Repository はデータアクセスだけに限定

### 動作確認

1. Home 側の登録ボタンと一覧表示が引き続き動く。
2. User 初期表示で一覧が出る。
3. User 名前検索で結果が変わる。
4. User 検索後は全画面リロードされる。
5. Validationエラー時の表示位置が分かる。

### ここで意図的に壊す

以下を1つだけ実施し、問題を確認後に戻す。

- Viewに業務判定を直接書く
- ControllerにSQL相当の処理を直接書く

### 壊した後の回収

- 責務を正しい層へ戻す
- 「なぜその層が適切か」を1行で説明できるようにする

### このSTEPの学び

- MVCの強みは速度より「責務の見通し」
- 変更箇所を予測しやすい構造は保守に効く

### 完了条件

- Home の基準線を維持したまま、User の全件表示と検索が安定動作
- フローを口頭で説明できる

---

## STEP 2: jQuery Ajax化で便利さと破綻の入口を体験（120分）

### 目的

部分更新の快適さを得つつ、責務分散が始まる瞬間を観測する。

### 実装タスク

1. 一覧表示を PartialView に切り出す。
2. POST /User/SearchAjax を追加し、PartialViewを返す。
3. 検索フォーム送信を jQuery Ajax に置き換える。
4. 成功時に結果領域だけ差し替える。

### 最小実装イメージ

```javascript
$.ajax({
  url: '/User/SearchAjax',
  method: 'POST',
  data: $('#searchForm').serialize(),
  success: function (html) {
    $('#result').html(html);
  }
});
```

### 動作確認

1. 検索時に画面全体リロードが起きない。
2. Network タブで Ajax 通信を確認できる。
3. 戻り値が HTML（Partial）であることを確認する。

### ここで意図的に壊す（必須）

次を必ず追加して、壊れやすさを体験する。

1. jQueryで表示切替を3箇所以上に増やす。
2. hidden fieldで状態を1つ保持する。
3. Validationの一部表示をJSで制御する。

### 観察ポイント

- 状態がどこに散ったか
- HTML変更時にJS修正が何箇所必要か
- 影響範囲の見積もりが難しくなったか

### 壊した後の回収

- 追加した表示切替の責務をメモ化
- どの責務を次STEPでhtmxへ戻すか決める

### 完了条件

- Ajax部分更新が動く
- 便利さと破綻の両方を説明できる

---

## STEP 3: htmxで通信責務をHTMLへ戻す（120分）

### 目的

通信配線をJSからViewへ戻し、更新箇所を追える状態にする。

### 実装タスク

1. 検索フォームに hx-post を付与する。
2. 更新先を hx-target で結果領域へ固定する。
3. 返却は PartialView とする。
4. 検索用の jQuery Ajax コードを削除する。

### 最小実装イメージ

```html
<form hx-post="/User/SearchHtmx"
      hx-target="#result"
      hx-swap="innerHTML">
  <input name="SearchName" />
  <button type="submit">検索</button>
</form>

<div id="result">
  @* _UserList *@
</div>
```

### 動作確認

1. 検索がhtmx経由で動く。
2. jQuery success関数なしで結果更新される。
3. 更新先DOMをHTML上で特定できる。

### ここで意図的に壊す（必須）

1. hx-target を複数箇所に増やす。
2. hx-trigger を複雑化する（keyup changed delay:300ms など）。

### 観察ポイント

- いつ通信されるか予測できるか
- どこが更新されるか追跡できるか
- 更新責務の見通しが崩れていないか

### 壊した後の回収

- 通信一覧表を作る（イベント / URL / target / swap）
- target命名を見直す

### 完了条件

- 検索更新をhtmxで運用できる
- 通信責務を文書で説明できる

---

## STEP 4: Alpine導入（UI状態だけ担当）（90分）

### 目的

サーバー責務を崩さず、UIローカル状態だけをAlpineで扱う。

### 実装タスク

1. 画面ルートに x-data を置く。
2. openDetail / isEditMode など UI状態のみ保持する。
3. 詳細本文は htmx で読み込む。
4. 編集モード切替は表示制御だけAlpineで行う。

### 最小実装イメージ

```html
<div x-data="{ openDetail: false, isEditMode: false }">
  <button @click="isEditMode = !isEditMode">編集モード</button>

  <button hx-get="/User/Detail/1"
          hx-target="#detailBody"
          @click="openDetail = true">
    詳細
  </button>

  <div x-show="openDetail">
    <div id="detailBody"></div>
  </div>
</div>
```

### 動作確認

1. モーダル開閉が動く。
2. 編集モード表示切替が動く。
3. 業務データがAlpine stateに入っていない。

### 注意

Validationメッセージや権限判定をAlpineに持ち込まない。
そこはサーバー責務のまま維持する。

### 完了条件

- UI状態と業務状態の分離を説明できる
- Alpineが「軽量UI補助」として機能している

---

## STEP 5: 破綻実験（このハンズオンの核心）（150分）

ここは必須工程です。
3つすべて実施してください。

### 破綻1: Alpine state 消失

実施:

- x-data 配下を htmx で丸ごと差し替える

観察:

- モーダルが閉じる
- 編集モードが初期化される
- チェック状態が消える

学び:

- htmxはHTML再生成
- Alpineはブラウザ内状態保持
- 境界設計を誤ると衝突する

回収:

- swap境界を x-data 外へ移動
- UI状態を持つノードを差し替え対象から外す

### 破綻2: Alpine肥大化

実施:

- x-dataに次を追加して意図的に肥大化させる
  - 権限フラグ
  - Validationエラー
  - 一時保存データ
  - 業務向け選択状態

観察:

- 可読性低下
- 状態遷移追跡困難
- 「小さなSPA自作」状態に近づく

学び:

- Alpineは業務状態管理の器ではない

回収:

- 業務状態をサーバー側へ戻す
- AlpineはUI補助プロパティだけに戻す

### 破綻3: htmx trigger 地獄

実施:

- 複数入力に hx-trigger="keyup changed delay:300ms" を設定

観察:

- 通信回数予測が困難
- 二重通信
- チラつき

学び:

- triggerは最小限が原則
- 監視対象は意図を持って限定する

回収:

- triggerを削減
- 通信一覧表を更新

### STEP 5 完了条件

- 3つの破綻を再現できる
- 原因と回収方法を説明できる

---

## STEP 6: 回収して最終構成に固定（120分）

### 目的

破綻学習を反映し、長期保守できる1つの成果物として完成させる。

### 回収タスク

1. Alpineから業務状態を完全除去する。
2. htmx更新領域を命名規約で整理する。
   - resultRegion
   - detailRegion
   - validationRegion
3. PartialViewを責務単位で分割する。
   - 一覧全体
   - 一覧行
   - 詳細
   - エラー表示
4. 通信トリガーを必要最小限に減らす。

### 最終責務定義（固定版）

- サーバー
  - 業務ルール
  - Validation
  - 権限制御
  - DB更新
- htmx
  - 部分更新の配線
  - HTML差し替え
- Alpine
  - 開閉
  - 表示切替
  - 一時的UI補助

### Definition of Done

- 初見メンバーが10分で更新責務を説明できる
- 検索条件追加時の修正箇所を事前予測できる
- 状態消失バグの再現と回避策を説明できる
- Validation表示責務の境界が明確

---

## 各STEP共通チェックリスト

毎STEP終了時に、次を全て確認する。

- どの層が何を担当するか1行で言える
- 状態保存場所が増えすぎていない
- ViewとJSの責務境界が明確
- 仕様変更時の影響範囲を説明できる

---

## 学習ログテンプレ（毎STEP終了時に必須）

1. 何を変更したか
2. 何が楽になったか
3. 何が壊れやすくなったか
4. 次STEPで守るルール
5. 今日の設計判断（1つ）

---

## 初心者向けの詰まりポイント集

### 症状: htmxで更新されない

確認順:

1. URLが正しいか
2. target要素IDが存在するか
3. ControllerがPartialViewを返しているか
4. ブラウザNetworkでレスポンスが200か

### 症状: Alpine状態が勝手に消える

確認順:

1. x-data配下を丸ごとswapしていないか
2. swap境界を分離できるか
3. 状態保持ノードを差し替え対象外にできるか

### 症状: 通信が多すぎる

確認順:

1. hx-triggerの設定数
2. keyup系の多用有無
3. 同一入力に重複イベントがないか

---

## このハンズオンで最終的に身につけること

- MVCは古いのではなく、責務の骨格として強い
- htmxは魔法ではなく HTTP + HTML更新
- Alpineは軽量UI補助であり、業務状態の器ではない
- 破綻はライブラリのせいではなく責務配置の問題

---

## 発展課題（完走後）

- htmxでValidationエラーHTMLを返す設計を作る
- swap境界再設計でAlpine state維持を改善する
- 通信頻度を計測しtrigger設計を最適化する
- CSRFトークン連携を確認する
- htmx history cacheの挙動差を確認する
- ViewModel責務をさらに分離する

---

## 最後に

ライブラリはコード削減の魔法ではありません。

本当に削減すべきものは次の4つです。

- 認知負荷
- 責務混乱
- 状態管理の破綻
- デバッグ困難

このハンズオン2の到達点は、
「MVC + htmx + Alpineを使えた」ではなく、
「どこに責務を置けば壊れにくいかを説明できる」です。

