@model List<User>

@{
    // ViewData["Title"] = "Home Page";
}

@* <a asp-action="Create" class="btn btn-primary">Create User</a> *@
<a asp-controller="Home" asp-action="Create" class="btn btn-primary">登録</a>

<h1>User List</h1>
<ul>
    @foreach (var user in Model)
    {
        <li>@user.UserName (@user.Company?.CompanyName)</li>
    }
</ul>

<h2>@ViewBag.Message</h2>

<a asp-action="Detail" asp-route-id="5">Detail 5へ</a>