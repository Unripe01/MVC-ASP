@model ReportPreviewViewModel

<div id="previewRegion" class="preview-panel">
    <div class="section-heading">
        <p class="eyebrow">Preview</p>
        <h2>txtプレビュー</h2>
    </div>

    <pre class="preview-text">@Model.PreviewText</pre>
</div>